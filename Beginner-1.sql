-- SQLite
SELECT * -- Select all columns
FROM movies -- from the table 'movies'
WHERE movie_release_year > 2020 -- Keep only values greater than 2020 for the 'movie_release_year' column
-- There are 168 movies
